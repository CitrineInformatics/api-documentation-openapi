type: object
description: A query against dataset metadata and/or PIFs
properties:
    logic:
        $ref: logic.yaml
    simple:
        $ref: simple.yaml
    dataset:
        type: array
        items:
            $ref: ../../dataset/query/DatasetQuery.yaml
        nullable: true
        default: null
        description: A list of queries to run against the content of datasets.
    system:
        type: array
        items:
            $ref: ../../pif/query/PifSystemQuery.yaml
        nullable: true
        default: null
        description: A list of queries to run against the content of PIFs.
    query:
        type: array
        items:
            $ref: DataQuery.yaml
        nullable: true
        default: null
        description: Nested data queries. Queries can nest as deeply as needed in order to create complex combinations of boolean logical operations.
